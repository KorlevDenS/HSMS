global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/hsms-alerts.yml

scrape_configs:
  - job_name: 'spring'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['spring:8177']
